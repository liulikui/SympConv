# 编码规范

## 1. 文件命名
- 所有文件名采用 PascalCase 命名规范，例如 `Vec3.h`、`RigidBody.cpp`
- 头文件使用 `.h` 扩展名，源文件使用 `.cpp` 扩展名

## 2. 函数命名
- 所有函数以大写字母开头，采用动词开头的命名方式
- 函数名中间不使用下划线，例如 `ApplyForce` 而非 `apply_force`

## 3. 类命名
- 类名采用 PascalCase 命名规范，例如 `RigidBody`、`DistanceConstraint`

## 4. 变量命名
- 成员变量使用m开头，驼峰命名，例如 `mPosition`、`mVelocity`
- 局部变量使用小写字母开头，驼峰命名
- 常量使用全大写，下划线分隔，例如 `PI`、`GRAVITY`

## 5. 代码风格
- 缩进使用 4 个空格
- 大括号使用新行开始和结束
- 操作符前后添加空格
- 所有的if和else分支都必须使用{}来包起来，即使只有一行代码

## 6. 头文件保护
- 使用 `#ifndef` 和 `#define` 进行头文件保护
- 宏名称使用全大写，下划线分隔，例如 `VEC3_H`

## 7. 注释规范
- 类和函数使用 Doxygen 风格注释
- 关键算法和复杂逻辑添加详细注释
- 注释使用中文或英文，保持一致

## 8. 命名空间
- 所有类型都放到 `SympConv` 命名空间中
- 命名空间使用 PascalCase 命名规范

## 9. 其他规范
- 避免使用魔法数字，使用命名常量
- 函数参数尽量使用 const 引用
- 遵循 RAII 原则管理资源
- 保持代码简洁，避免过度复杂的逻辑